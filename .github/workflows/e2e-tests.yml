name: E2E Tests

on:
  pull_request:
    branches: [main]  # Only run real MetaMask tests on PRs to main

env:
  NODE_VERSION: '20'
  CI: true

jobs:
  security-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Security audit
        run: npm audit --audit-level=high
        continue-on-error: true

      - name: Run TypeScript checks
        run: npm run typecheck

      - name: Run ESLint
        run: npx eslint . --ext .ts,.tsx --max-warnings 0
        continue-on-error: true

  test-metamask:
    needs: security-check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium

      - name: Install xvfb
        run: sudo apt-get install -y xvfb

      - name: Start development server
        run: npm run dev &

      - name: Wait for server
        run: |
          timeout 60 bash -c 'until curl -f http://localhost:5173; do sleep 2; done'

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v47

      - name: Build Synpress cache
        run: xvfb-run -a npm run test:cache

      - name: Run signin tests (if signin changed)
        if: contains(steps.changed-files.outputs.all_changed_files, 'signIn')
        run: xvfb-run -a npm run test:signin-conditions

      - name: Run auth tests (if auth changed)
        if: contains(steps.changed-files.outputs.all_changed_files, 'auth')
        run: xvfb-run -a npm run test:auth-flow

      - name: Run all tests (if no specific match)
        if: |
          !contains(steps.changed-files.outputs.all_changed_files, 'signIn') &&
          !contains(steps.changed-files.outputs.all_changed_files, 'auth')
        run: xvfb-run -a npm run test:all-metamask

      - name: Format test results
        if: failure()
        run: |
          echo "## ❌ MetaMask Tests Failed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Failed Tests:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          # Extract only failed test names and errors
          grep -E "(✕|Error:|Expected:|Received:|at .*\.test\.ts)" test-results/results.txt 2>/dev/null || true
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-metamask
          path: |
            playwright-report/
            test-results/
          retention-days: 7

  test-browser:
    needs: security-check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium

      - name: Start development server
        run: npm run dev &

      - name: Wait for server
        run: |
          timeout 60 bash -c 'until curl -f http://localhost:5173; do sleep 2; done'

      - name: Run browser compatibility tests
        run: npm run test:browser

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-browser
          path: |
            playwright-report/
            test-results/
          retention-days: 7

  test-summary:
    needs: [test-metamask, test-browser]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Test Summary
        run: |
          echo "## Test Results 🧪" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.test-metamask.result }}" = "failure" ]; then
            echo "### ❌ MetaMask Tests: FAILED" >> $GITHUB_STEP_SUMMARY
            echo "Check the MetaMask test logs for details" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.test-metamask.result }}" = "success" ]; then
            echo "### ✅ MetaMask Tests: PASSED" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ needs.test-browser.result }}" = "failure" ]; then
            echo "### ❌ Browser Tests: FAILED" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.test-browser.result }}" = "success" ]; then
            echo "### ✅ Browser Tests: PASSED" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📊 View detailed reports in the artifacts section" >> $GITHUB_STEP_SUMMARY